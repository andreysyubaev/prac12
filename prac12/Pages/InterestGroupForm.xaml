<Page x:Class="prac12.Pages.InterestGroupForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prac12.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InterestGroupForm">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="80,0" VerticalAlignment="Center">
            <StackPanel>
                <Label>Название группы</Label>
                <TextBox Text="{Binding Title}"/>
            </StackPanel>

            <StackPanel>
                <Label>Описание</Label>
                <TextBox Text="{Binding Description}"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="Back_Click" Content="Назад"/>
            <Button Grid.Column="1" Click="Save_Click" Content="Сохранить"/>
        </Grid>
    </Grid>
</Page>
