<Page x:Class="prac12.Pages.UserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prac12.Pages"
      xmlns:validadors="clr-namespace:prac12.Validators"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserPage">
    <Page.Resources>
        <validadors:LoginValid x:Key="loginValid"/>
        <validadors:EmailValid x:Key="emailValid"/>
        <validadors:PasswordValid x:Key="passwordValid"/>
    </Page.Resources>
    <Grid>
        <StackPanel Margin="80,0" VerticalAlignment="Center">
            <Label Content="Добавление/изменение пользователя"/>
            <StackPanel>
                <TextBlock Text="Логин"/>
                <TextBox x:Name="Login" Validation.ErrorTemplate="{x:Null}">
                    <TextBox.Text>
                        <Binding Path="Login"
                                 UpdateSourceTrigger="PropertyChanged"
                                 ValidatesOnDataErrors="True"
                                 NotifyOnValidationError="True">
                            <Binding.ValidationRules>
                                <validadors:LoginValid/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=Login,
                    Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Имя"/>
                <TextBox Text="{Binding Name}"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Почта"/>
                <TextBox x:Name="Email" Validation.ErrorTemplate="{x:Null}">
                    <TextBox.Text>
                        <Binding Path="Email"
                                 UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validadors:EmailValid/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=Email,
                    Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Пароль"/>
                <TextBox x:Name="Password" Validation.ErrorTemplate="{x:Null}">
                    <TextBox.Text>
                        <Binding Path="Password"
                                 UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validadors:PasswordValid/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=Password,
                    Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label x:Name="ValidError" Content="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Подтверждение пароля"/>
                <TextBox x:Name="ConfirmPassword"/>
            </StackPanel>

            <Button Click="Save_Click" Content="Сохранить"/>
            <Button Click="Back_Click" Content="Назад"/>
        </StackPanel>
    </Grid>
</Page>
