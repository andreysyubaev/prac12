<Page x:Class="prac12.Pages.RoleForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prac12.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RoleForm">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Margin="80,0" VerticalAlignment="Center">
                <StackPanel>
                    <Label>Название роли</Label>
                    <TextBox Text="{Binding Title}"/>
                </StackPanel>
            </StackPanel>

            <ListView Grid.Column="1"
                      ItemsSource="{Binding Students}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="Фамилия "/>
                            <Run Text="{Binding LastName}"/>
                            <Run Text="Имя "/>
                            <Run Text="{Binding FirstName}"/>
                            <Run Text="Отчетсво "/>
                            <Run Text="{Binding MiddleName}"/>
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>

        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="Back_Click" Content="Назад"/>
            <Button Grid.Column="1" Click="Save_Click" Content="Сохранить"/>
        </Grid>
    </Grid>
</Page>
